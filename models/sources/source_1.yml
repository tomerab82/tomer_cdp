version: 2

sources:
  - name: source_1
    description: "This is source_1 for the project"
    database: snowflake_sample_data
    schema: tpch_sf1
    tables:
      - name: orders
        description: "This is the orders table"
      - name: lineitem
        description: "This is the lineitem table"